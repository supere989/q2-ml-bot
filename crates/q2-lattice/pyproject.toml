[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "q2-lattice-rs"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
features = ["python"]
module-name = "q2_lattice_rs"
